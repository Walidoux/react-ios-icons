import path from "node:path"

export const ICONS_DIR = path.join(process.cwd(), 'src', 'icons')
export const README_PATH = path.join(process.cwd(), 'README.md')
export const PLACEHOLDER_ICON_COUNT = '<!-- ICON_COUNT -->'
