import { Icon } from '../Icon'
import type { {{ properCase name }}Props, IconProps } from '../IconProps'

export const {{ properCase name }}: IconProps<{{ properCase name }}Props> = ({
  className,
  defaultProp,
  ...rest
}) => <Icon className={className} {...rest}></Icon>
