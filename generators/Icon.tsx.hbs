import { Icon } from '../Icon'
import type { IconProps } from '../IconProps'

export const {{ properCase name }}: IconProps = ({ className, ...rest }) => (
  <Icon className={className} {...rest}></Icon>
)
