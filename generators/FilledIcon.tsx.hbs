import { Icon } from '../Icon'
import type { IconProps } from '../IconProps'

export const {{ properCase name }}: IconProps<unknown, true> = ({
  className,
  filled,
  ...rest
}) => <Icon className={className} {...rest}></Icon>
